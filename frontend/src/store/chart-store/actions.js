import {cloneDeep} from 'lodash'
export const chartOptionAction = (context,payload) => {



  context.commit('chartOptionMutation','')
}

export const chartSeriesAction = (context,payload) => {


  context.commit('chartSeriesMutation','')
}
